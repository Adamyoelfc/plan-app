/* ====== CONFIGURACIÓN ======
   Pega tu API key de RapidAPI (ExerciseDB) entre las comillas para que
   se muestren los GIF de cada ejercicio. Si lo dejas vacío, el preview
   cae automáticamente a un botón de video (nunca se rompe). */
const CONFIG = {
  RAPIDAPI_KEY: "",                         // <-- tu key aquí
  EDB_HOST: "exercisedb.p.rapidapi.com",
  PRO_TARGET: 150,                          // gramos de proteína al día
  CAL_TARGET: 2040                          // calorías al día
};
